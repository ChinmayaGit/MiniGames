# Assets

| Path | Use |
| --- | --- |
| `icons/flip.svg` | Uno Flip card mark (loaded by `unoflip.html`) |
| `images/cubestacc-deck-ref.png` | Cubestacc deck / cube art reference |

Game HTML stays at the repo root so Netlify URLs (`/Uno.html`, `/dobble`, …) stay simple. Shared CSS/JS lives in `../shared/`.
